services:
  - type: web
    name: tw-stock-analyzer
    env: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: ACCESS_TOKEN
        sync: false
